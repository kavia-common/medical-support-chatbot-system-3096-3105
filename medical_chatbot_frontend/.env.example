# Frontend environment configuration for Medical Chatbot Frontend

# PUBLIC_INTERFACE
# REACT_APP_API_BASE_URL
# Base URL of the FastAPI backend. In this environment, use the proxy URL below.
# Example for local dev: http://localhost:8000
# Example for this environment (proxy):
REACT_APP_API_BASE_URL=https://vscode-internal-30348-beta.beta01.cloud.kavia.ai/proxy/8001

# Optional site URL for redirects if needed by backend flows (not used currently)
# REACT_APP_SITE_URL=
